#include <stdio.h>

int main(int argc, char **argv){

	int print_inp(){
		int i;

		for( i = 0; i < argc; i++ ) {
		printf("Arg %d = (%s) \n", i, argv[i]);
    		}

    		return 0;
		}
	print_inp(argc, argv);
	return 0;
}


